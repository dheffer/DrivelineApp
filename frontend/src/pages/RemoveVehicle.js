function RemoveVehicle() {
    return (
        <div>
            <h1>Remove Vehicle</h1>
            <p>This is the Remove Vehicle page.</p>
            <p>There shouldn't be anything here other than a confirmation</p>
        </div>
    )
}

export default RemoveVehicle;